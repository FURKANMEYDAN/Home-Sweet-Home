<link rel="stylesheet" href="~/css/_Footer.css">
<footer class="footer" id="footer">
    <div class="footer-left">
        <div class="katalog">
            <h3>Keşfet</h3>
            <ul id="wrapper">
                <li><a href="#" class="button" id="Magaza">Mağazalarımız</a></li>
                <li><a href="#" class="button" id="Katalog">Kataloglarımız</a></li>
                <li><a asp-controller="Detaylar" asp-action="Odalar" class="button" id="Duzenle">Odanıza Göre
                        Seçimler</a></li>
            </ul>

        </div>

    </div>
    <div class="footer-center">
        <h3>Geri Bildirim</h3>
        <p>Görüşleriniz bizim için değerli bize geri dönüşte bulunun!</p>
        @if (User.Identity.IsAuthenticated)
        {
           
            <a asp-action="Feedback" asp-controller="Feedback">Geri Dönüşte Bulun</a>
        }
        else
        {

            <button id="geridonus" onclick="return sendAlert()">Geri Dönüşte Bulun</button>
        }
    </div>
    <div class="footer-right">
        <ul class="social-media">
            <div class="contact">
                <h3>İletişim</h3>
                <i class="fa fa-linkedin-square fa-3x" aria-hidden="true"></i>
                <i class="fa fa-instagram fa-3x" aria-hidden="true"></i>
                <i class="fa fa-github fa-3x" aria-hidden="true"></i>
                <ul style="list-style-type: none;">
                    <li><a id="linkedin" href="https://www.linkedin.com/in/furkanmeydan">/furkanmeydan</a></li>
                    <li><a id="instagram" href="https://www.instagram.com/">/instagram</a></li>
                    <li><a id="github" href="https://github.com/FURKANMEYDAN">/github/FURKANMEYDAN</a></li>
                </ul>


            </div>
        </ul>
    </div>
</footer>
<script >
    function sendAlert(input){
    var button = document.getElementById("geridonus");
      var tiklandi = false;
    button.addEventListener("click",function(){
    tiklandi = true;
});
var isAuthenticated = @Html.Raw(User.Identity.IsAuthenticated.ToString().ToLower());
if(!isAuthenticated){
    alert("Lütfen önce giriş yapınız!");
    return false;
}
    return true;
}
</script>